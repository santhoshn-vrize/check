'use strict';

/**
 * default-menubar controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::default-menubar.default-menubar');
